[package]
name = "orx-concurrent-recursive-iter"
version = "3.0.0"
edition = "2024"
authors = ["orxfun <orx.ugur.arikan@gmail.com>"]
description = "A concurrent iterator that can be extended recursively by each of its items."
license = "MIT OR Apache-2.0"
repository = "https://github.com/orxfun/orx-concurrent-recursive-iter/"
keywords = ["concurrency", "iterator", "iteration", "recursive", "parallelism"]
categories = ["data-structures", "concurrency", "rust-patterns", "no-std"]

[dependencies]
crossbeam-deque = { version = "0.8.7", default-features = false, optional = true, features = [
    "std",
] }
crossbeam-queue = { version = "0.3.13", default-features = false, features = [
    "alloc",
] }
orx-concurrent-iter = { version = "4.0.0", default-features = false }
orx-pseudo-default = { version = "2.1.0", default-features = false, optional = true }
orx-pinned-vec = { version = "4.0.0", default-features = false, optional = true }
orx-fixed-vec = { version = "4.0.0", default-features = false, optional = true }
orx-split-vec = { version = "4.0.0", default-features = false, optional = true }
orx-concurrent-queue = { version = "2.0.0", default-features = false, optional = true }

[dev-dependencies]
clap = { version = "4.6.6", features = ["derive"] }
criterion = { version = "0.8.2", default-features = false }
enum-iterator = { version = "2.3.0", default-features = false }
orx-concurrent-bag = { version = "4.0.0", default-features = false }
orx-criterion = { version = "1.0.0" }
rayon = { version = "1.12.0" }
rand_chacha = { version = "0.10.0" }
rand = { version = "0.10.2" }
test-case = { version = "3.3.1" }

[features]
default = ["std"]
std = ["dep:crossbeam-deque"]
experimental = [
    "dep:orx-pseudo-default",
    "dep:orx-pinned-vec",
    "dep:orx-split-vec",
    "dep:orx-fixed-vec",
    "dep:orx-concurrent-queue",
]

# benchmarks

[[bench]]
name = "reduction"
harness = false
